name: Docker Publish

on:
  workflow_run:
    workflows: ["Go CI"]
    types:
      - completed

jobs:
  docker:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Extract version
        id: version
        run: |
          if git describe --tags --abbrev=0 > /dev/null 2>&1; then
            VERSION=$(git describe --tags --abbrev=0)
          else
            VERSION=dev
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Set lowercase owner and repo
        id: lowercase
        run: |
          echo "OWNER=$(echo $GITHUB_REPOSITORY_OWNER | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT
          REPO=$(echo "${GITHUB_REPOSITORY#*/}" | tr '[:upper:]' '[:lower:]')
          echo "REPO=$REPO" >> $GITHUB_OUTPUT
        shell: bash

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ steps.lowercase.outputs.OWNER }}/${{ steps.lowercase.outputs.REPO }}:latest
            ghcr.io/${{ steps.lowercase.outputs.OWNER }}/${{ steps.lowercase.outputs.REPO }}:${{ steps.version.outputs.version }}
            ghcr.io/${{ steps.lowercase.outputs.OWNER }}/${{ steps.lowercase.outputs.REPO }}:${{ github.sha }}
