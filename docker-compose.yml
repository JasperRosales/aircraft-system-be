services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aircraft-api
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    working_dir: /app
    environment:
      GOOSE_DRIVER: "postgres"
      GOOSE_DBSTRING: "postgresql://postgres.nhwpditkyufxixofaxkr:Skurohoshi26@aws-1-ap-south-1.pooler.supabase.com:5432/postgres"
      GOOSE_MIGRATION_DIR: "./database/migrations"
    command: go run ./cmd/api
